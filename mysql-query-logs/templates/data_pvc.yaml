apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ include "mysql-query-logs.fullname" . }}-datadir-pvc"
spec:
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

